# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
*.tsbuildinfo

# Turborepo build cache — was previously tracked in git (63MB of .tar.zst
# cache blobs, none of which need to survive a clone); this rule stops it
# from happening again after the untrack below.
.turbo

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel
.env*.local

# Bank integration reference docs — contains real UAT credentials, never commit
/API documents/
/PayChain Tarrif/
/apps/Paychain Paybill Sticker..pdf
