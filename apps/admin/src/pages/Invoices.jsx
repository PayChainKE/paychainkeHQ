import React, { useState, useEffect, useCallback } from 'react';
import Layout from '../components/layout/Layout';
import api from '../api/api';

const fmtKES = (n) => {
  const v = typeof n === 'string' ? parseFloat(n) : n;
  if (!Number.isFinite(v)) return '0.00';
  return v.toLocaleString(undefined, { minimumFractionDigits: 2, maximumFractionDigits: 2 });
};

const fmtDate = (iso) => iso
  ? new Date(iso).toLocaleDateString('en-GB', { day: '2-digit', month: 'short', year: 'numeric' })
  : '—';

const STATUS_META = {
  draft: { label: 'Draft', tone: 'bg-amber-50 text-amber-700 border-amber-200' },
  sent:  { label: 'Sent',  tone: 'bg-blue-50 text-blue-700 border-blue-200' },
  paid:  { label: 'Paid',  tone: 'bg-emerald-50 text-emerald-700 border-emerald-200' },
  void:  { label: 'Void',  tone: 'bg-slate-100 text-slate-500 border-slate-200' },
};

const STATUS_TABS = ['all', 'draft', 'sent', 'paid', 'void'];

const Invoices = () => {
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState('');
  const [invoices, setInvoices] = useState([]);
  const [summary, setSummary] = useState({ draft: { count: 0, totalAmount: 0 }, sent: { count: 0, totalAmount: 0 }, paid: { count: 0, totalAmount: 0 }, void: { count: 0, totalAmount: 0 } });

  const [status, setStatus] = useState('all');
  const [searchInput, setSearchInput] = useState('');
  const [q, setQ] = useState('');
  const [page, setPage] = useState(1);
  const [totalPages, setTotalPages] = useState(1);
  const [total, setTotal] = useState(0);
  const limit = 25;

  const fetchInvoices = useCallback(async () => {
    setLoading(true);
    setError('');
    try {
      const res = await api.get('/api/admin/invoices', { params: { page, limit, status, q } });
      if (res.data?.success) {
        setInvoices(res.data.invoices || []);
        setSummary(res.data.summary || summary);
        setTotalPages(res.data.totalPages || 1);
        setTotal(res.data.total || 0);
      } else {
        setError(res.data?.error || 'Could not load invoices.');
      }
    } catch (err) {
      setError(err?.response?.data?.error || err?.message || 'Could not load invoices.');
    } finally {
      setLoading(false);
    }
  }, [page, status, q]); // eslint-disable-line react-hooks/exhaustive-deps

  useEffect(() => { fetchInvoices(); }, [fetchInvoices]);

  useEffect(() => {
    const t = setTimeout(() => { setQ(searchInput.trim()); setPage(1); }, 400);
    return () => clearTimeout(t);
  }, [searchInput]);

  const totalInvoiced = summary.sent.totalAmount + summary.paid.totalAmount;
  const totalCollected = summary.paid.totalAmount;

  return (
    <Layout>
      <div className="space-y-8 pb-12">
        {/* Header Hero */}
        <div className="relative overflow-hidden rounded-3xl bg-[#061121] border border-[#1A2639] shadow-[0_30px_80px_-20px_rgba(6,17,33,0.8)] p-6 md:p-10">
          <div className="absolute -top-32 -right-32 w-[500px] h-[500px] bg-emerald-500/10 rounded-full blur-[100px]"></div>
          <div className="absolute -bottom-24 -left-24 w-[400px] h-[400px] bg-blue-500/10 rounded-full blur-[80px]"></div>

          <div className="relative flex flex-col md:flex-row md:items-end md:justify-between gap-6">
            <div>
              <div className="flex items-center gap-3 mb-3">
                <span className="relative flex h-3 w-3">
                  <span className="animate-ping absolute inline-flex h-full w-full rounded-full bg-emerald-400 opacity-75"></span>
                  <span className="relative inline-flex rounded-full h-3 w-3 bg-emerald-500"></span>
                </span>
                <p className="text-[11px] font-bold uppercase tracking-[0.3em] text-emerald-400">Platform-Wide Billing</p>
              </div>
              <h2 className="text-[32px] md:text-[48px] font-bold text-white tracking-tighter font-headline leading-tight">
                Invoices
              </h2>
              <p className="text-[13px] md:text-[15px] text-blue-100/60 mt-2 max-w-2xl font-body">
                Every invoice generated by every merchant on PayChain — drafts, sent, paid, and voided.
              </p>
            </div>
            <button
              onClick={fetchInvoices}
              disabled={loading}
              className="group self-start md:self-auto flex items-center gap-2 px-5 py-3 bg-white/5 hover:bg-white/10 backdrop-blur-md border border-white/10 text-white text-[12px] font-bold rounded-xl uppercase tracking-widest transition-all disabled:opacity-50"
            >
              <span className={`material-symbols-outlined text-[18px] group-hover:rotate-180 transition-transform duration-500 ${loading ? 'animate-spin' : ''}`}>sync</span>
              Refresh
            </button>
          </div>
        </div>

        {/* Summary Cards */}
        <div className="grid grid-cols-2 lg:grid-cols-4 gap-4">
          <div className="bg-surface-container-lowest p-5 rounded-2xl border border-outline-variant/20 shadow-sm transition-transform hover:-translate-y-1">
            <span className="text-[11px] font-bold uppercase tracking-[0.15em] text-on-surface-variant/50 block mb-2">Total Invoices</span>
            <span className="text-3xl md:text-4xl font-black text-on-surface tracking-tighter tabular-nums">{total}</span>
          </div>
          <div className="bg-gradient-to-br from-[#0B1512] to-[#06201B] p-5 rounded-2xl border border-emerald-900/40 shadow-lg transition-transform hover:-translate-y-1">
            <span className="text-[11px] font-bold uppercase tracking-[0.15em] text-emerald-100/50 block mb-2">Collected (Paid)</span>
            <div className="flex items-baseline gap-2">
              <span className="text-2xl md:text-3xl font-black text-white tracking-tighter tabular-nums">KES {fmtKES(totalCollected)}</span>
            </div>
            <span className="text-[10px] text-emerald-400 font-bold">{summary.paid.count} invoices</span>
          </div>
          <div className="bg-surface-container-lowest p-5 rounded-2xl border border-outline-variant/20 shadow-sm transition-transform hover:-translate-y-1">
            <span className="text-[11px] font-bold uppercase tracking-[0.15em] text-on-surface-variant/50 block mb-2">Outstanding (Sent)</span>
            <span className="text-2xl md:text-3xl font-black text-on-surface tracking-tighter tabular-nums">KES {fmtKES(summary.sent.totalAmount)}</span>
            <div className="mt-1"><span className="text-[10px] text-blue-600 font-bold">{summary.sent.count} invoices</span></div>
          </div>
          <div className="bg-surface-container-lowest p-5 rounded-2xl border border-outline-variant/20 shadow-sm transition-transform hover:-translate-y-1">
            <span className="text-[11px] font-bold uppercase tracking-[0.15em] text-on-surface-variant/50 block mb-2">Drafts</span>
            <span className="text-2xl md:text-3xl font-black text-on-surface tracking-tighter tabular-nums">{summary.draft.count}</span>
            <div className="mt-1"><span className="text-[10px] text-amber-600 font-bold">Not yet sent</span></div>
          </div>
        </div>

        {/* Table */}
        <div className="bg-surface-container-lowest rounded-3xl border border-outline-variant/20 shadow-editorial overflow-hidden">
          <div className="px-6 py-5 border-b border-outline-variant/10 bg-white/50 backdrop-blur-xl flex flex-col sm:flex-row sm:items-center justify-between gap-4">
            <div>
              <h3 className="text-lg font-black text-on-surface tracking-tight">All Invoices</h3>
              <p className="text-[12px] text-on-surface-variant/60 font-medium">Across every merchant account</p>
            </div>
            <div className="flex flex-col sm:flex-row items-stretch sm:items-center gap-3">
              <div className="relative">
                <span className="material-symbols-outlined absolute left-3 top-1/2 -translate-y-1/2 text-[16px] text-slate-400">search</span>
                <input
                  value={searchInput}
                  onChange={(e) => setSearchInput(e.target.value)}
                  placeholder="Invoice #, customer name or email..."
                  className="pl-9 pr-3 py-2 border border-outline-variant/40 rounded-lg text-[12px] font-medium bg-white w-full sm:w-64"
                />
              </div>
              <div className="flex gap-1 p-1 bg-white border border-outline-variant/20 rounded-xl overflow-x-auto no-scrollbar">
                {STATUS_TABS.map(s => (
                  <button
                    key={s}
                    onClick={() => { setStatus(s); setPage(1); }}
                    className={`px-3 py-1.5 text-[10px] font-black uppercase tracking-widest rounded-lg whitespace-nowrap transition-all ${
                      status === s ? 'bg-[#06201B] text-white' : 'text-slate-500 hover:text-primary'
                    }`}
                  >
                    {s === 'all' ? 'All' : STATUS_META[s]?.label || s}
                  </button>
                ))}
              </div>
            </div>
          </div>

          {error ? (
            <div className="p-6 text-center text-red-500 text-sm font-bold">{error}</div>
          ) : (
            <div className="overflow-x-auto custom-scrollbar">
              <table className="w-full text-left font-body min-w-[900px]">
                <thead>
                  <tr className="bg-surface-container-low/30 border-b border-outline-variant/10">
                    <Th className="pl-6">Invoice</Th>
                    <Th>Merchant</Th>
                    <Th>Customer</Th>
                    <Th className="text-right">Amount</Th>
                    <Th>Status</Th>
                    <Th>Issued</Th>
                    <Th className="text-center pr-6">Paid</Th>
                  </tr>
                </thead>
                <tbody className="divide-y divide-outline-variant/5">
                  {loading ? (
                    <tr><td colSpan="7" className="px-6 py-16 text-center">
                      <div className="inline-flex flex-col items-center gap-4">
                        <div className="relative w-12 h-12">
                          <div className="absolute inset-0 rounded-full border-2 border-emerald-500/20"></div>
                          <div className="absolute inset-0 rounded-full border-2 border-emerald-500 border-t-transparent animate-spin"></div>
                        </div>
                        <span className="text-[13px] font-bold text-on-surface-variant uppercase tracking-widest">Loading invoices...</span>
                      </div>
                    </td></tr>
                  ) : invoices.length === 0 ? (
                    <tr><td colSpan="7" className="px-6 py-12 text-center text-on-surface-variant/40 text-sm font-medium">No invoices match this filter.</td></tr>
                  ) : invoices.map((inv) => <InvoiceRow key={inv._id} inv={inv} />)}
                </tbody>
              </table>
            </div>
          )}

          {!loading && !error && totalPages > 1 && (
            <div className="px-6 py-4 border-t border-outline-variant/10 flex items-center justify-between">
              <p className="text-[11px] font-bold text-on-surface-variant/60">Page {page} of {totalPages} • {total} total</p>
              <div className="flex items-center gap-2">
                <button
                  onClick={() => setPage(p => Math.max(1, p - 1))}
                  disabled={page === 1}
                  className="w-8 h-8 rounded-full flex items-center justify-center bg-white border border-outline-variant/20 text-primary shadow-sm hover:bg-primary hover:text-white disabled:opacity-20 transition-all"
                >
                  <span className="material-symbols-outlined text-lg">chevron_left</span>
                </button>
                <button
                  onClick={() => setPage(p => Math.min(totalPages, p + 1))}
                  disabled={page >= totalPages}
                  className="w-8 h-8 rounded-full flex items-center justify-center bg-white border border-outline-variant/20 text-primary shadow-sm hover:bg-primary hover:text-white disabled:opacity-20 transition-all"
                >
                  <span className="material-symbols-outlined text-lg">chevron_right</span>
                </button>
              </div>
            </div>
          )}
        </div>
      </div>
    </Layout>
  );
};

const InvoiceRow = ({ inv }) => {
  const meta = STATUS_META[inv.status] || { label: inv.status, tone: 'bg-slate-100 text-slate-500 border-slate-200' };
  return (
    <tr className="hover:bg-surface-container-lowest transition-colors bg-white">
      <td className="px-6 py-4">
        <span className="text-[13px] font-mono font-bold text-on-surface">#{inv.invoiceNumber}</span>
      </td>
      <td className="px-3 py-4">
        <span className="text-[13px] font-bold text-on-surface tracking-tight">{inv.merchant?.businessName || '—'}</span>
        <p className="text-[11px] text-on-surface-variant/50">{inv.merchant?.email}</p>
      </td>
      <td className="px-3 py-4">
        <span className="text-[13px] font-medium text-on-surface">{inv.customer?.name || 'Unnamed Customer'}</span>
        <p className="text-[11px] text-on-surface-variant/50 truncate max-w-[180px]">{inv.customer?.email || '—'}</p>
      </td>
      <td className="px-3 py-4 text-right">
        <span className="text-[14px] font-bold text-on-surface tabular-nums">{inv.currency} {fmtKES(inv.total)}</span>
      </td>
      <td className="px-3 py-4">
        <span className={`inline-flex items-center px-2.5 py-1 rounded-full text-[10px] font-bold uppercase tracking-widest border ${meta.tone}`}>
          {meta.label}
        </span>
      </td>
      <td className="px-3 py-4">
        <span className="text-[12px] font-medium text-slate-500">{fmtDate(inv.issueDate || inv.createdAt)}</span>
      </td>
      <td className="px-6 py-4 text-center">
        {inv.status === 'paid' ? (
          <span className="inline-flex items-center gap-1 text-emerald-600" title={fmtDate(inv.paidAt)}>
            <span className="material-symbols-outlined text-[18px]">check_circle</span>
          </span>
        ) : (
          <span className="text-[11px] text-slate-300">—</span>
        )}
      </td>
    </tr>
  );
};

const Th = ({ children, className = '' }) => (
  <th className={`px-3 py-4 text-[10px] font-black uppercase tracking-[0.15em] text-slate-400 ${className}`}>{children}</th>
);

export default Invoices;
