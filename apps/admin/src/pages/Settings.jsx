import React from 'react';
import Layout from '../components/layout/Layout';

export default function Settings(){
  return (
    <Layout>
      <div className="pc-page-head"><h2>Settings</h2></div>
      <div className="pc-section">Settings area (mock). TODO: wire real settings API.</div>
    </Layout>
  );
}
